Reversing an array from the given array of integers

function reverseArray(a) {
   
    const reverseArray = []
    
    for(var i = a.length - 1; i >= 0; i--){
        reverseArray.push(a[i]);
    }
    return reverseArray;
}
reverseArray(a);
